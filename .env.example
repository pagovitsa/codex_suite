# Copy to .env only when changing the defaults.
BIND_ADDRESS=127.0.0.1
HTTP_PORT=3400
# Absolute Windows paths use forward slashes, e.g. C:/Projects/my-project
# This folder must already exist. Mount only the project the agent may change.
PROJECT_PATH=./workspace
